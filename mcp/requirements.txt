# Python 3.11+ required
#
# Server-mode dependency set: the shared runtime core, plus local embeddings
# and dev tooling. The desktop build installs desktop/requirements.txt, which
# pulls in requirements-core.txt without the heavy pieces below.

-r requirements-core.txt

# ── Embeddings (server only) ──────────────────────────────────────────────────
# CPU-only torch index first so sentence-transformers doesn't pull the multi-GB CUDA build.
# Without this constraint the image balloons to 5GB+ and nodes run out of ephemeral storage.
# Even CPU-only, this block plus its transitive deps (scipy, sympy, sklearn,
# transformers) is ~770MB installed — which is why the desktop app uses API
# or Ollama embeddings instead.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
sentence-transformers>=2.3.1

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# ── Type checking ─────────────────────────────────────────────────────────────
mypy>=1.5.0
types-python-dateutil>=2.8.0
